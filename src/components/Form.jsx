import PropTypes from "prop-types";

import "./form.css";

function Form({  }) {
  return (
   <div className="storybook-form"></div>
  )
}

Form.propTypes = {
  backgroundColor: PropTypes.string,
}

export default Form